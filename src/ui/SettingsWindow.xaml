<Window x:Class="TranslatorCsV2.Ui.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Translator"
        Width="440"
        Height="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#0E0F13"
        Foreground="#F2F3F5"
        FontFamily="Segoe UI"
        FontSize="13">
    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="Label">
            <Setter Property="Foreground" Value="#8A8F9C"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1A1C22"/>
            <Setter Property="Foreground" Value="#F2F3F5"/>
            <Setter Property="BorderBrush" Value="#2A2D36"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="#1A1C22"/>
            <Setter Property="Foreground" Value="#F2F3F5"/>
            <Setter Property="BorderBrush" Value="#2A2D36"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#1A1C22"/>
            <Setter Property="Foreground" Value="#F2F3F5"/>
            <Setter Property="BorderBrush" Value="#2A2D36"/>
            <Setter Property="Padding" Value="10,6"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3B5BDB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="18,9"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>

    <Grid Margin="22,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Translator" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16"/>

        <TextBlock Grid.Row="1" Text="OPENAI API KEY" Style="{StaticResource Label}"/>
        <PasswordBox Grid.Row="2" x:Name="ApiKeyBox" Margin="0,0,0,16"/>

        <TextBlock Grid.Row="3" Text="HOTKEY" Style="{StaticResource Label}"/>
        <TextBox Grid.Row="4"
                 x:Name="HotkeyBox"
                 IsReadOnly="True"
                 Text="click to set"
                 GotFocus="OnHotkeyFocus"
                 LostFocus="OnHotkeyBlur"
                 Cursor="Hand"
                 Margin="0,0,0,16"/>

        <TextBlock Grid.Row="5" Text="SOURCE LANGUAGE" Style="{StaticResource Label}"/>
        <ComboBox Grid.Row="6" x:Name="SourceBox" Margin="0,0,0,16"/>

        <TextBlock Grid.Row="7" Text="TARGET LANGUAGE" Style="{StaticResource Label}"/>
        <ComboBox Grid.Row="8" x:Name="TargetBox" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="10" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Background="#2A2D36" Click="OnCancel" Margin="0,0,8,0"/>
            <Button Content="Save" Click="OnSave"/>
        </StackPanel>
    </Grid>
</Window>
